program SomaSimples;
var
  dec, hex, oct: integer;
begin
  dec := 20;
  hex := 0x20;
  oct := 020;
  write("dec = ");
  writeln(dec);
  write("hex = ");
  writeln(hex);
  write("oct = ");
  writeln(oct);
end.
